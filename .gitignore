# Dependencies
node_modules/

# Build output
dist/
build/

# Local env files (never commit secrets)
.env
.env.local
.env.*.local

# Backend raw PDFs — not the vector index. The index IS committed
# so the free-tier server can serve answers without a persistent disk.
backend/data/pdfs/
backend/data/*.tmp

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Test / scratch
.scratch/
coverage/
