node_modules/
.env
.env.local
.env.*.local

# Raw uploaded PDFs are big — never commit. The vector index (vectors.json)
# IS committed so the free-tier server can serve answers without a disk.
data/pdfs/
data/*.tmp

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
