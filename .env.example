# React frontend — environment example.
# Copy to `.env.local` for local dev; on Vercel/Netlify set as an env var.

# URL of your deployed RAG backend (the Node.js project under /backend).
# Leave blank to call same-origin (useful when proxied).
VITE_API_BASE=https://millet-vista-rag.onrender.com
